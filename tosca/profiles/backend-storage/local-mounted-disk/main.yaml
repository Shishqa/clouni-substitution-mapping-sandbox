tosca_definitions_version: tosca_simple_yaml_1_3

node_types:
  clouni.nodes.mountedDisk.local:
    derived_from: tosca.nodes.SoftwareComponent
    properties:
      mount_path:
        type: string
    attributes:
      storage_uri:
        type: string
[Unit]
Description=Minio server run

[Service]
Type=simple
Environment=MINIO_ROOT_USER={{ minio_root_user }} MINIO_ROOT_PASSWORD={{ minio_root_password }}
ExecStart=minio server {{ data_dir }} --console-address '{{ console_address }}:{{ console_port }}'

[Install]
WantedBy=multi-user.target
